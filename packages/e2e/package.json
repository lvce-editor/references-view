{
  "name": "e2e",
  "version": "0.0.0-dev",
  "license": "MIT",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "e2e": "npm run e2e:prepare && test-with-playwright --only-extension=. --test-path=.",
    "e2e:headless": "npm run e2e:prepare && test-with-playwright --only-extension=. --test-path=. --headless",
    "e2e:prepare": "node ./scripts/prepare-extension.js",
    "type-check": "tsc"
  },
  "devDependencies": {
    "@lvce-editor/api": "^8.49.0",
    "@lvce-editor/test-with-playwright": "^12.1.0",
    "esbuild": "^0.28.1"
  }
}
